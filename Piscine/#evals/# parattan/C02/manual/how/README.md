# Help Pages

Place your help pages here as plain text files.

Naming pattern:

- `git-clone` (for `how git clone`)
- `nano` (for `how nano`)
- `make-build` (for `how make build`)

Use dashes to separate multi-word commands.
